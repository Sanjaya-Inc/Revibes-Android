<task>
  <goal>Collect all findings from this &lt;session/git diff&gt;, including &lt;additional_focus_item&gt;.</goal>

  <actions>
    <step>1. Update AGENTS.md — add rules + references pointing to /revibes-design-system and /revibes-architecture skill</step>
    <step>2. Update all skills mentioned above — move all heavy logic/knowledge into it; AGENTS.md stays thin</step>
    <step>4. Make sure no duplicated information</step>
    <step>5. Verify: AGENTS.md = rules + skill refs only; skill = all and SKILL.md should only have rules and references, and references files will do heavylifting.</step>
    <step>6. Run /optimize-prompt audit on every block you added</step>
  </actions>
</task>